diff --git a/kairo-sidecar/sidecar/masters/word_master.py b/kairo-sidecar/sidecar/masters/word_master.py
index a123456..b789101 100644
--- a/kairo-sidecar/sidecar/masters/word_master.py
+++ b/kairo-sidecar/sidecar/masters/word_master.py
@@ -206,8 +206,8 @@ class WordContextExtractor:
             if re.match(numbered_pattern, p.text):
                 has_numbered_sections = True
                 break
 
-        # Average sentence length
-        all_text = "\n".join(p.text for p in doc.paragraphs if p.text.strip())
+        # Average sentence length - sample first 50 paragraphs to avoid performance bottleneck in large documents
+        all_text = "\n".join(p.text for p in doc.paragraphs[:50] if p.text.strip())
         sentences = [s.strip() for s in re.split(r"[.!?]", all_text) if s.strip()]
         avg_sentence_len = 0
         if sentences:
